# 安全提示

## 重要提醒

### 1. 保护你的会话文件
- `session/storage_state.json` 包含你的登录凭证和 cookies
- **切勿**将此文件提交到 git 或分享给他人
- 已在 `.gitignore` 中排除此文件

### 2. 保护 API 密钥
- 如果使用 AI 分析功能，`config.json` 包含 API 密钥
- **切勿**将此文件提交到版本控制
- 使用 `config.example.json` 作为模板

### 3. 合理使用
- 遵守闲鱼平台的使用条款和 robots.txt
- 设置合理的请求间隔，避免频繁请求
- 不要用于商业爬取或恶意用途
- 仅用于个人学习和研究

### 4. 数据隐私
- 导出的数据可能包含卖家信息
- 妥善保管导出的数据文件
- 不要公开分享他人的个人信息

### 5. 账号安全
- 定期更换密码
- 如发现异常登录，立即删除 `session/storage_state.json` 并重新登录
- 不要在公共设备上使用本工具

## 检查清单

在开源或分享代码前，请确保：

- [ ] 已删除或排除 `session/storage_state.json`
- [ ] 已删除或排除 `config.json`（如果包含真实 API 密钥）
- [ ] 已删除 `data/` 目录中的导出文件（可能包含隐私信息）
- [ ] 已删除日志文件 `debug.log` 和 `server.log`
- [ ] 检查代码中没有硬编码的密钥或敏感信息

## 报告安全问题

如果你发现安全漏洞，请通过私密方式联系维护者，不要公开披露。
